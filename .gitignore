node_modules/
dist/
.astro/
.netlify/
.vercel/
.env
.vercel
.env.backup*
.DS_Store

# Private archive (QASR 3D models etc.) — NEVER commit while the repo is public
archive/

shopify-app/.shopify/

# local dev-server config carries the Theme Access token — never commit
.claude/launch.json
