---
// Shared frame for all /admin pages: sidebar, auth-guarded content area.
interface Props { title: string; active: string }
const { title, active } = Astro.props;
const nav = [
  ['dashboard', '/admin/', 'Dashboard'],
  ['properties', '/admin/properties/', 'Properties'],
  ['blog', '/admin/blog/', 'Journal'],
  ['content', '/admin/content/', 'Site Content'],
  ['sections', '/admin/sections/', 'Page Sections'],
  ['lists', '/admin/lists/', 'Services & More'],
  ['leads', '/admin/leads/', 'Leads Inbox'],
  ['settings', '/admin/settings/', 'Settings'],
  ['help', '/admin/help/', 'Help & Guide'],
];
---
<!DOCTYPE html>
<html lang="en-AU">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta name="robots" content="noindex, nofollow">
<title>{title} · Sabdia Admin</title>
<link rel="stylesheet" href="/css/style.css">
<link rel="stylesheet" href="/css/admin.css">
</head>
<body class="admin">
<div class="ad-app">
  <aside class="ad-side">
    <div class="ad-side-logo">
      <img src="/images/logo.png" alt="Sabdia">
      <span>Admin Portal</span>
    </div>
    <button type="button" class="ad-cmd-open" id="adCmdOpen" title="Search everything — ⌘K">
      <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6"><circle cx="11" cy="11" r="7"/><path d="m20 20-3.8-3.8"/></svg>
      <span>Search</span>
      <kbd>⌘K</kbd>
    </button>
    <nav class="ad-nav">
      {nav.map(([key, href, label]) => (
        <a href={href} class={active === key ? 'on' : ''}>{label}</a>
      ))}
      <a href="/" target="_blank">View Website ↗</a>
    </nav>
    <div class="ad-side-foot">
      <div class="ad-side-user" id="adUser"></div>
      <button class="ad-logout" id="adLogout" type="button">Sign out</button>
    </div>
  </aside>
  <main class="ad-main">
    <slot />
  </main>
</div>
<script>
  import { initAdminSearch } from '../lib/admin/search.js';
  initAdminSearch();
</script>
</body>
</html>
